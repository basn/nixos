diff --git a/tools/terminal_scope.py b/tools/terminal_scope.py
index 0f56f78..7a31372 100644
--- a/tools/terminal_scope.py
+++ b/tools/terminal_scope.py
@@ -10,6 +10,7 @@ the profile's COMPLETE ``TERMINAL_*`` policy; while bound, ``terminal_env`` res
 
 from __future__ import annotations
 
+import json
 import logging
 import os
 from contextlib import contextmanager
@@ -142,6 +143,28 @@ def build_profile_terminal_scope(hermes_home: "Any") -> Dict[str, str]:
         raw_terminal = raw.get("terminal") if isinstance(raw, dict) else None
         if isinstance(raw_terminal, dict):
             _apply(raw_terminal)
+
+    # Cron output is the only Hermes state exposed to terminal sandboxes. Resolve
+    # the source from this turn's profile home so multiplexed profiles cannot see
+    # one another's jobs, reports, credentials, or session databases.
+    if scope.get("TERMINAL_ENV") == "docker":
+        output_dir = (home / "cron" / "output").resolve()
+        if output_dir.is_dir():
+            try:
+                volumes = json.loads(scope.get("TERMINAL_DOCKER_VOLUMES", "[]"))
+            except (TypeError, ValueError, json.JSONDecodeError):
+                # Preserve the terminal backend's existing actionable error for
+                # malformed user configuration.
+                volumes = None
+            if isinstance(volumes, list):
+                destination_in_use = any(
+                    isinstance(volume, str)
+                    and (":/cron-output:" in volume or volume.endswith(":/cron-output"))
+                    for volume in volumes
+                )
+                if not destination_in_use:
+                    volumes.append(f"{output_dir}:/cron-output:ro")
+                    scope["TERMINAL_DOCKER_VOLUMES"] = json.dumps(volumes)
     return scope
 
 
diff --git a/cron/scheduler.py b/cron/scheduler.py
index e833a41..659856a 100644
--- a/cron/scheduler.py
+++ b/cron/scheduler.py
@@ -3115,2 +3115,10 @@ def _launch_external_cron_worker(job: dict) -> bool:
     worker_env = systemd_user_bus_env(worker_env)
+    # The child-environment sanitizer deliberately removes Hermes-owned
+    # PYTHONPATH entries. This packaged overlay is the cron runtime itself, so
+    # re-add its root after sanitization: the restart-safe worker must import
+    # the same patched scheduler and terminal scope as its gateway parent.
+    runtime_root = str(Path(__file__).resolve().parent.parent)
+    worker_env["PYTHONPATH"] = os.pathsep.join(
+        part for part in (runtime_root, worker_env.get("PYTHONPATH", "")) if part
+    )
     try:
diff --git a/tools/cronjob_tools.py b/tools/cronjob_tools.py
index dbb77c1..39f58c0 100644
--- a/tools/cronjob_tools.py
+++ b/tools/cronjob_tools.py
@@ -337,8 +337,8 @@ def _run_claimed_job(job: Dict[str, Any], extra_prompt: Optional[str] = None) ->
         return {"claimed": True, "success": False, "error": str(e)}
 
 
-def _latest_job_output_excerpt(job_id: str, max_chars: int = 2000) -> Optional[str]:
-    """Excerpt of the job's most recent saved output file for the background completion
+def _latest_job_output_excerpt(job_id: str, max_chars: int = 12000) -> Optional[str]:
+    """Final response from the job's most recent output file for the background completion
     block (parent sees what the job produced). Never raises."""
     try:
         from cron.jobs import get_cron_output_dir
@@ -346,8 +346,15 @@ def _latest_job_output_excerpt(job_id: str, max_chars: int = 2000) -> Optional[
         text = files[-1].read_text(encoding="utf-8", errors="replace").strip() if files else ""
         if not text:
             return None
+        # Saved documents begin with the fully assembled prompt, which can be
+        # tens of kilobytes. Return the agent's report rather than exhausting
+        # the completion preview before reaching it.
+        response_marker = "\n## Response\n\n"
+        if response_marker in text:
+            text = text.split(response_marker, 1)[1].strip()
         if len(text) > max_chars:
-            text = text[:max_chars] + f"\n… (truncated; full output: {files[-1]})"
+            sandbox_path = f"/cron-output/{job_id}/{files[-1].name}"
+            text = text[:max_chars] + f"\n… (truncated; full output: {sandbox_path})"
         return text
     except Exception:
         return None
